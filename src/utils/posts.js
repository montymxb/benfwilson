// Auto-generated file - do not edit manually
// Run 'npm run build-posts' to regenerate

export const posts = [
  {
    "slug": "adding-a-ci-runner-to-forgejo",
    "title": "Adding a CI Runner to Forgejo",
    "date": "2026-04-18",
    "excerpt": "Setting up a tiny OLED driver to work within 1kb of flash & 64 bytes of SRAM on an ATTiny13",
    "content": "Just a quick one here. I found myself needing to setup a Gitforge locally some months ago to serve as a pre-staging area for development (and local hosting for private projects as well).\nThere were a a few good choices to start with, being [Gitlab](https://about.gitlab.com/), [Forgejo](https://forgejo.org/) & [Gitea](https://about.gitea.com/).\nOf these I've already setup a Gitea & Gitlab instance in the past, but I was really keen on trying out Forgejo this time around.\nForgejo itself is based on Gitea as well, and having had a good experience with that, I was quite curious to see how that would pan out.\n\n<!-- - Running Forgejo for a local git forge\n    - Akin to a pre-staging area\n    - Also nice for private projects, such as dotfiles that contain a little more than I'd like to push up somewhere else\n    - Running Immich too, but that's a separate topic -->\n\nIn case you're not familiar, a Git forge is a platform for developing & collaborating on software [^wiki], and a Git forge does this around git itself naturally.\nIn general, most Forge's come with rich features beyond providing a web-based Git client as well, such as issue tracking, CI support, and even package management.\nForgejo in particular has all of these features, which was a prime factor in motivating me to try it out.\nTo be fair, Gitea & Gitlab also support these, but the proximity of Forgejo's UI to Github's is also particularly attractive.\nAnything to reduce the onboarding time & friction is always a plus.\n\n[^wiki]:https://en.wikipedia.org/wiki/Forge_(software)\n\n<!-- - Noted that as a forge it has really rich capabilities\n    - Able to add ci runners\n    - able to add package support too (for a variety of package managers)\n    - overall great stuff -->\n\nSo, in this one I wanted to explore how I setup a CI runner in Forgejo, which was really quite interesting.\nI ran into a number of hiccups along the way (mostly due to my own setup).\nDuring the process of tracing my steps, I figured I could just write it up in a post later as well; especially so I could refer to it later on.\n\nOn my end my Forgejo instance is running on an old 2015 MacBook pro, which is running Debian. According to the [documentation for installing Forgejo runners](https://forgejo.org/docs/v11.0/admin/actions/runner-installation/), it's recommended to put the runner on a separate system. A major part of this is security related, but additionally there's the notion of performance as well. Putting the runner on an entirely different system keeps a nice division between the concerns, and prevents a bad CI job (or jobs) from taking down your whole forge.\n\nAs for the CI runner itself, I opted to dust-off another macbook pro from 2011 that hasn't seen much use in a while. The hard drive was replaced with an SSD, and it has mismatched RAM of 2 GB + 4 GB (long story for another time, but in short don't do that). This machine has been running Arch for a bit as an experiment, so it was more or less ready to start setting up as a runner.\n\n<!-- - So I figured I would put in a runner, but I ran into some quirks along the way, so figured I would write this\n    - Forgejo is running on an old 2015 mac running Debian\n    - The runner should be on a separate system due to the issues w/ arbitrary code execution for jobs, recommended to keep that separate (especially with Docker's root thing)\n        - although Podman is supported too from what I can see\n    - Runner was then planned on an even older 2011 mac book pro that's running Arch\n    - More of a bespoke machine here, but it's done wonders since I swapped it over to Arch -->\n\nAs noted above for installing runners, Forgejo has some great instructions for setting up runners. For Arch in particular, there's also a `forgejo-runner` package in the **extra** repo, which I opted to start with. There's also a package in the the AUR, but I ran into some issues setting that up.\n\n<!-- - Forgejo has some great instructions for setting up runners\n    - There's also a forgejo-runner package in the extra repo, which looks to be fairly good\n    - Opted for the AUR package, but ran into a few problems while setting it up -->\n\n### Installation\n\nIn case you haven't handled it already, `docker` and `git` need to be setup in advance (assuming you don't them installed already):\n\n```bash\nsudo pacman -Syu forgejo-runner docker git\n```\n\nDocker also needs to be enabled as well.\n\n```bash\nsudo systemctl enable --now docker.service\n```\n\nIn my case the `forgejo-runner` user was created automatically, but it's good to double check that it's there first. If not you can [make it yourself as well](https://forgejo.org/docs/v11.0/admin/actions/runner-installation/#setting-up-the-runner-user).\n\nOnce the runner is all setup, you'll need a registration token from your Forgejo instance (local or otherwise).\nThis is pretty straightforward, since it can be grabbed by hopping over to **/admin/actions/runners** at your Forgejo instance and setting up a new runner.\nIt just needs a name and a description, and in return you'll get your registration token.\nBe sure to hold onto that, as you'll need it later when configuring a **.runner** file via `forgejo-runner register`[^forgejo-register].\nWe'll get to that part after we ensure Forgejo itself is ready.\n\n[^forgejo-register]:https://forgejo.org/docs/v11.0/admin/actions/runner-installation/#standard-registration\n\nYou'll also need to tweak the config shortly. This includes deciding whether you require docker-in-docker jobs. That requires `container.privileged` to be set, and probably some other details, but I left this off on my end, so I can't attest to it.\n\nOnce you do have your token and your runner is configured to your liking (*note* the runner from the extras repo for Arch sets its config under /etc/, not under /var/...) you can register it.\n\nBe sure that your forgejo instance is also securely accessible.\nFor a publicly accessible forge this is likely to be an upfront concern that you've already resolved via LetsEncrypt or a similar service.\nHowever, for a homelab setup a self-signed cert won't cut it when trying to hookup the runner.\n\nFor some background, on my end my instance was configured & managed via Ansible, and also setup with self-signed certs.\nThis worked initially, but the runner wouldn't be able to connect without a legitimate certificate authority (CA) to trust.\n\n### Let's Make a CA\n\nSo, I made a CA to trust!\nThere's this wonderful project called `mkcert` [^mkcert] which provides a convenient solution to this very problem.\nIt's not suitable for non-testing or production systems, but for a local setup where you control the environment (mostly), and are willing to accept the risks & responsibility for doing this, it's a pretty convenient option (and it certainly beats self-signed certs).\n\n[^mkcert]:https://github.com/FiloSottile/mkcert\n\n```bash\n# create your CA\nmkcert -install\n# and create a new certificate for the following names\nmkcert you-forgejo-instance.local localhost 127.0.0.1\n```\n\nOf course, the names are up to you, as these are just some examples.\n\nThis will give you a cert & a key which you can use as legitimate certs, assuming the CA is trusted. Depending on how you're running Forgejo you can plug the cert & key into the config or into the **docker-compose.yml** itself. _If_ you are using docker-compose (like me) then you're going to have to also update/recreate the volume as well, since it gets populated on creation.\n\nOnce that's in place you'll want to make sure Forgejo actually pulled them in & is using your new cert correctly. If it's bare this is easier, but with docker it's also straightforward:\n```bash\ndocker compose down\ndocker compose up -d\n```\n\nYou could also force a recreate as well if it's not taking, but that should be sufficient.\n\nNow that you've gotten your Forgejo instance to use your custom cert, you'll need to start getting things ready for your runner.\nIn particular, you'll need to get your CA root, and pass it to your runner so you can trust it.\n\n```bash\n# get the location of the CA root\nmkcert -CAROOT\n```\n\nCopy that over to your runner system, and add it into `/etc/ca-certificates/trust-source/anchors/`.\nI named mine `mkcert-forgejo.crt`, but you can choose what works on your end.\n\n### Setting up the Runner\n\nOnce that's in place you'll want to rebuild your certificate bundle.\n```bash\nsudo trust extract-compat\n```\n\nNow that you have the trusted cert on your runner system, you still need to bind-mount the CA bundle into your jobs so that it's picked up.\nFrom what I was able to tell, this appears to be due to docker having its own separate CA file that it uses, but I didn't get around to verifying this directly.\n```bash\ncontainer:\n     valid_volumes:\n       - /etc/ssl/certs/ca-certificates.crt\n     options: \"-v /etc/ssl/certs/ca-certificates.crt:/etc/ssl/certs/ca-certificates.crt:ro\"\n```\n\nAnd now, using your token from before, you can proceed to register your new runner like so:\n\n```bash\nforgejo-runner register\n```\n\nFill out the details, and set whatever runner name you like.\nFor me I put in `arch-runner`, but it could have also been `potato-runner` to reflect the hardware in this case.\n\nOn my end the labels looked okay by default, but some of these are a bit dated.\nYou can tweak them pretty easily.\n\nYou should double check the systemd service at this point, to make sure it's running, registered, and that it's reading from the config you changed (and not the **/var/** one).\nUnless you configured it to explicitly point to a separate config.\nThis got me pretty bad since I was assuming the **/var** location was the used one, and although there was a config there I spent a bit too much time trying to figure out why changes weren't applying.\n```bash\nsystemctl cat forgejo-runner.service\n```\n\nYou can then enable the freshly registered runner, & check its status:\n```bash\nsudo systemctl enable --now forgejo-runner.service\nsudo systemctl status forgejo-runner.service\n```\n\nIt might be worth checking the jouralctl to make sure something didn't go wrong behind the scenes as well, just to cover your bases.\n```bash\nsudo journalctl -u forgejo-runner.service -f\n```\n\nYou'll also need to verify that you can actually resolve the name of your forgejo instance _from inside your runner's docker containers_.\n`/etc/hosts` won't cut it since that's just for your own system, and the runner will likely have its own resolution.\nThere are a number of ways to do this, almost all of which revolve around updating the forgejo-runner configuration (very similar to what was done with the cert bundle above).\n\nSomething like this will do it real quick.\n\n```bash\ncontainer:\n  options: \"--add-host=forgejo.lan:192.168.1.x\"\n```\n\nAlso good to restart the runner here to, given:\n```bash\nsudo systemctl restart forgejo-runner.service\n```\n\nBut, on my end I wanted this forgejo instance to be generally reachable across my network at `forgejo.lan`, not just this one machine. Thankfully, I already had a PiHole instance that I setup sometime later in 2025, and so I opted to add a custom DNS record for this very purpose.\nBy the way be careful about _what_ names you decide to set custom records for, as they could shadow legitimate names upstream (this applies to `.local` domains as well).\n\nAdding the record to PiHole was simple enough, but getting the container to pick up that same record took an extra step. DNS can be customized by modifying the daemon with `/etc/docker/daemon.json` (which doesn't exist normally). Creating that file (if it doesn't exist yet) and adding in some JSON with the following DNS servers did the trick:\n\n```json\n{\n  \"dns\": [\"192.168.2.X\", \"1.1.1.1\"]\n}\n```\n\nFollowed by a docker restart too:\n```bash\nsudo sysetmctl restart docker\n```\n\nAnd if you're still not sure that worked, you can verify it by trying a quick lookup inside an Alpine container as a test:\n```bash\ndocker run --rm alpine sh -c \"nslookup forgejo.lan\"\n```\n\nThis worked on my end, but I also did another quick verify via a `curl` request in a similar fashion.\n\n### Running CI Jobs\n\nAssuming that it all works, you can (more or less) push up a repo with either `.forgejo/workflows/ci.yml` or `.github/workflows/ci.yml` present, and it should fire up the job!\n\nIn my case most of these projects have a primary remote on Github, so I often had pre-existing Github workflows.\nAs far as I could tell these worked pretty much out of the box.\nI'm sure there are exceptions, but it seems the Forgejo runner follows pretty closely with their specification language & features, which is very convenient.\n\nAnd that was about it! At this point I had a custom Forgejo runner for CI jobs on an old macbook pro.\nIt didn't really need to be setup in such a way, but it was a lot of fun to do so, and allowed me to put the old machine to good use.\nBest of all when I don't need the runner online, the forge works just fine without it, so I can opt to turn it on as need be.\nHowever next time I think I'll pick up a more dedicated machine to do the job.\nEspecially when automated publishing is a goal (such as for npm packages).\n\n<!-- draft-below -->",
    "frontmatter": {
      "title": "Adding a CI Runner to Forgejo",
      "date": "2026-04-18",
      "tags": "[attiny, post, project, avr, oled]",
      "excerpt": "Setting up a tiny OLED driver to work within 1kb of flash & 64 bytes of SRAM on an ATTiny13",
      "draft": "false"
    }
  },
  {
    "slug": "driving-an-oled-display-on-an-attiny13",
    "title": "Driving an OLED Display from an ATTiny13",
    "date": "2026-04-18",
    "excerpt": "Setting up a tiny OLED driver to work within 1kb of flash & 64 bytes of SRAM on an ATTiny13",
    "content": "> [!NOTE]\n> _This post is a continuation from [debugging & monitoring on an ATTiny13](/post/attiny-getting-started-and-flashing). Check that out if you're interested in seeing how we got here._\n\n\nFrom the last time I was working on the ATTiny13, I was able to get to a bit-banged half-duplex UART setup (see the post link above for details on that). Albeit at a bit of a cost on the RX side. Already I removed that half, and was perfectly happy with a TX only implementation, but now I was really interested in hooking up some peripherals.\n\nI wasn't entirely sure yet, but I did have the following general sketch that I was going for (really it's just a sketch, but it's where all good ideas start from).\n\n![A very quick sketch of the device I have in mind at the end.](/assets/images/board-sketch.png)\n\nAlthough it's quite crude, this still summarizes the general idea of what I'm going for. A small prototyping device with 3 or 4 button inputs, a rotary encoder of some form, a small screen, external power & GPIOs exposed. Still, this is a very rough idea, and it's subject to change based on how things come together.\n\nButtons are straightforward enough. Simply connecting GPIOs to ground and enabling the internal pull-ups on the ATTiny13 for example, but already I was pretty limited on GPIOs to begin with (we have 6 at most). That, plus I really wanted to get a screen in there too, and that was going to be _much_ trickier. I had a small OLED with an I2C interface that I wanted to try out in particular. However I2C is _not_ supported on the ATTiny13 natively, but seems to be supported on the 45 last I checked, and so it looked like I was going to need to setup another bit-banged implementation to make this work too.\n\nLast, but not least, it was already evident that there was not going to be enough flash or memory on-chip to handle everything I had in mind. I would definitely need to hookup some external flash & memory later on. I could simply get a bigger chip with more than enough head-room (ex. an ATMega328), but personally for me having such constraints creates a much more interesting (and creatively conducive) problem space to work within.\n\n## Wiring up the OLED\n\nI was really keen on getting this I2C OLED display working first, so I started familiarizing myself with the I2C protocol. I'm not an embedded systems engineer by trade (I tend to be more on the parsers, type-checkers and compilers side of things) so although I know about most of these protocols, I don't know much about the implementation details (at least not from memory). A quick browse on the wiki page for the I2C protocol revealed info that was pretty helpful [^i2c-wiki].\n\nWe would need a couple of signal pins: an SDA (serial data line) and an SCL (serial data clock). Already we were only using 2 pins for half-duplex UART, and without RX support that was really just 1 pin. So there were pins to spare still.\n\n[^i2c-wiki]: I2C Reference: https://en.wikipedia.org/wiki/I2C\n\nIt was at this point that I went _back_ to my [original post](/post/attiny-getting-started-and-flashing) to look at the ATTiny13 pin-out again. I recalled writing an SCL there, and if so that meant I didn't need to setup a bit-banged implementation for the ATTiny13. The SDA & SCL pins _were_ listed, but they were written using the spec for the ATTiny45 and not the ATTiny13. This wasn't an issue, but I just wanted to be sure I wasn't missing a preexisting implementation on the 13.\n\nSo, I whipped up some simplistic I2C bit-banging code using pins PB2 & PB3.\n\n```c\n#define SDA_PIN PB2\n#define SCL_PIN PB3\n// microseconds, ~100kHz\n#define I2C_DELAY 5\n\n// set SDA/SCL as outputs (low)\n#define SDA_LOW() DDRB |= (1 << SDA_PIN)\n#define SCL_LOW() DDRB |= (1 << SCL_PIN)\n\n// set SDA/SCL as inputs (pulled high)\n#define SDA_HIGH() DDRB &= ~(1 << SDA_PIN)\n#define SCL_HIGH() DDRB &= ~(1 << SCL_PIN)\n\n// read SDA state\n#define SDA_READ() (PINB & (1 << SDA_PIN))\n\nvoid i2c_start(void) {\n    SDA_HIGH();\n    SCL_HIGH();\n    _delay_us(I2C_DELAY);\n    SDA_LOW();\n    _delay_us(I2C_DELAY);\n    SCL_LOW();\n}\n\nvoid i2c_stop(void) {\n    SDA_LOW();\n    _delay_us(I2C_DELAY);\n    SCL_HIGH();\n    _delay_us(I2C_DELAY);\n    SDA_HIGH();\n    _delay_us(I2C_DELAY);\n}\n\n// send 8 bits, MSB first\nuint8_t i2c_write_byte(uint8_t data) {\n    for (uint8_t i = 0; i < 8; i++) {\n        if (data & 0x80) {\n            SDA_HIGH();\n        } else {\n            SDA_LOW();\n        }\n        _delay_us(I2C_DELAY);\n        SCL_HIGH();\n        _delay_us(I2C_DELAY);\n        SCL_LOW();\n        data <<= 1;\n    }\n\n    // read ACK bit\n    SDA_HIGH();\n    _delay_us(I2C_DELAY);\n    SCL_HIGH();\n    _delay_us(I2C_DELAY);\n    // ACK is active low\n    uint8_t ack = !SDA_READ();\n    SCL_LOW();\n\n    return ack;\n}\n\nuint8_t i2c_read_byte(uint8_t ack) {\n    uint8_t data = 0;\n    SDA_HIGH();\n\n    // read 8 bits\n    for (uint8_t i = 0; i < 8; i++) {\n        _delay_us(I2C_DELAY);\n        SCL_HIGH();\n        _delay_us(I2C_DELAY);\n        data <<= 1;\n        if (SDA_READ()) {\n            data |= 1;\n        }\n        SCL_LOW();\n    }\n\n    // send ACK/NACK\n    if (ack) {\n        SDA_LOW();\n    } else {\n        SDA_HIGH();\n    }\n    _delay_us(I2C_DELAY);\n    SCL_HIGH();\n    _delay_us(I2C_DELAY);\n    SCL_LOW();\n    SDA_HIGH();\n\n    return data;\n}\n```\n\nThis was already looking good for a first attempt, and corresponded nicely to the I2C specification I read earlier.\n\nI also soldered on some header pins for the little OLED display, and set it up on its own breadboard for testing.\n\n![A blue/black 1.3\" 128x64 OLED Display based on the SH1106. And yes, the peel is still on.](/assets/images/oled1.jpg)\n\nI powered it on as a test, but didn't see anything light up, which makes sense given it's an OLED and there's no 'backlight' panel that would light up.\n\n> [!TIP]\n> It's always good to double check your voltages. I first checked the data-sheet for this particular display to ensure it wasn't expecting 3.3.v power instead of 5v. The one I happen to have is based on the SH1106. It has its own voltage regulator onboard, and can accept either 3.3.v or 5v to drive it.\n\nI wired up SDA to PB2, and SCL to PB3. I also hooked up a couple of 5k pull-up resistors from SDA & SCL to VCC, and I was about ready to try out the code I wrote up. \n\n![A roughly wired up OLED display to an obscured ATTiny13 on the other board](/assets/images/oled2.jpg)\n\n## Interfacing with the SH1106\n\nHowever I still needed to get the I2C address for the device, which is required in order to correctly identify which device you want to talk with. In many cases we may have a common bus for SDA & SCL between numerous devices, all communicating over I2C. So long as the addresses are unique across all devices, data can be transmitted to the correct device.\n\nAt first, I didn't really know what the address was, so I started looking up information about the SH1106. Unfortunately I wasn't able to pin down the _exact_ data-sheet for this board. Despite that setback, I did manage to find a likely default address of `0x3C`. Even if that was wrong I could work around that, and see if I could sketch up some code to update a single pixel on the screen.\n\nAfter looking around even more, I was still coming up short for a data-sheet on the SH1106. Instead, I came across an [SH1106 implementation by Adafruit](https://github.com/davidperrenoud/Adafruit_SH1106) that looked quite promising, although it was a bit dated (11 years at the time of writing).\n\n### Digging for info\n\nBacking up a bit, I figured that I should start looking more into Adafruit's product line. Not only did they have a very similar component for sale, but it seemed like they had some solid demo code as well. Specifically, I came across a [1.3\" 128x64 OLED graphic display with an SSD1306](https://www.adafruit.com/product/938) (wrong driver chip, but similar in its execution from what I could tell), complete with some example code too. Mainly these were targeting Arduino board, but that brought up another idea entirely. I figured I could instead do a test run using an Arduino with some known-good libraries to get a working state.\n\n### Going the Arduino Route\n\nGoing this route, I pulled out an old Arduino Uno that I had lying around, stole my printer's USB-B cable to hook it up, and wired it to the OLED board. Thankfully I had already separately mounted the OLED board on a separate half-size breadboard. This proved to be pretty helpful, making it easy to pull it over to my Uno without the mess of wires that was accumulating around the ATTiny13's board.\n\n![Arduino Uno wired up to the OLED board for testing. Nevermind the messy probe attachments, that part comes later.](/assets/images/oled3.jpg)\n\nI used a very simple example program leveraging Wire (Arduino's 2-wire ISP comms library), and an Adafruit graphics library for talking with the SH1006 specifically (right driver this time around). I wasn't planning to stick with an Arduino, but assuming it worked it would be pretty easy to derive a minimally working implementation for the ATTiny13 from that.\n\nWhile going this route I also came across the i2c_scanner example for Arduino boards, which allowed me to quickly confirm that the address for the OLED board was in fact `0x3C` and that the _board was responding_ to requests!\n\n> [!NOTE]\n> `0x3C` was expected, but it could have also been `0x3D`, always good to double check!\n\nThis was super helpful, as it was likely now that only my I2C implementation was flawed, rather than having a dead OLED board.\n\nFrom there I installed a library for one of [SH1106 examples](https://github.com/laura240406/SH1106) (one of their snowflake examples) and gave the associated example program a test.\n\n![Working snowflake demo on the OLED. A great way to confirm the device was working as expected.](/assets/images/snowflake.jpg)\n\nSo now that I had a baseline that was working, my plan was to pivot back from that to establish a smaller, simpler implementation on the ATTiny13.\n\nYet, there was a key issue, graphics take memory & program space. My hope, and plan, was that I could start simply, without needing external storage. From there, I could _probably_ use external memory or flash to store prepared sequences that I could stream to the OLED, without exceeding the memory or program space on the ATTiny13. But either way I needed to cognizant of the overhead.\n\n## Backporting a Working Implementation onto the ATTiny13\n\nAfter about a week of other things occupying my time, I came back to this and picked up porting that baseline I2C OLED driver setup. I started with the basic Arduino implementation, and got to work on the port. Ultimately, I wanted to setup the display logic to show a few pixels on the screen, akin to this code pulled from the original Arduino example:\n\n```c\n// init the display\ndisplay.begin();\n// draw a pixel\ndisplay.drawPixel(10,10, WHITE);\n// show it\ndisplay.display();\n// clear it out after 5 seconds\ndelay(5000);\ndisplay.clearDisplay();\n```\n\nIf I could get this part ported over, then I would have a nice starting point to build from.\n\nThe first thing I wanted to move over was the `display.begin` init code. I had looked a bit up, and started sketching a few attempts, but didn't get anywhere at first. A lot of that was quite dependent on the information I had at hand for the SCH1106, and I still kept having issues locating the right data-sheet, but I made do with what I had at hand.\n\nAfter this I wanted to look at how `display.drawPixel` was setup. This method had a bit of rotation detection logic (which I chose to skip over) and then wrote the pixel value into a buffer as `0x1` for white (blue in my case) & `0x0` for black. Writing a pixel just populated the internal buffer, so I skipped the rest. As is I knew I couldn't even have a model buffer on the ATTiny13 given the memory constraint, at least not without some external memory attached.\n\nI wondered if `clearDisplay` was just clearing the internal buffer to all black & calling `display.display`, so I took a peek. Turns out that one was even simpler than I expected, just a single call to `memset` on the buffer & nothing else. So I was able to skip that entirely.\n\nThe last piece of the implementation was `display.display`. Right off the bat I could see that the data being sent was divided up into pages, of which there were 8. This seemed to correlate pretty well with what I saw back in `drawPixel`. In that method there was also a notion of computing the following for the buffer:\n```c\n// set a pixel 'white'\nbuffer[x + (y / 8) * SH1106_LCDWIDTH] |= (1 << (y & 7));\n```\n\nSince we have a 128x64 display, and we're calculating y / 8, we get 8 pages. So this is effectively using `x + curPage * WIDTH`, where `WIDTH` multiplied by our page allows us to get computed offsets in a flat array. Beyond that I looked into how they stored their data, but more or less I had what I needed to proceed.\n\n### Writing up the SH1106 Driver\n\nThe first part of `display.display` was mostly setup code:\n```c\n// set low col\nsh1106_cmd(0x00);\n// set high col\nsh1106_cmd(0x10);\n// set start line\nsh1106_cmd(0x40);\n\n// set page address (0xB0-0xB7 for pages 0-7)\nsh1106_cmd(0xB0 + page);\n\n// set column address (statically for now)\nuint8_t col = 2;\n// low nibble\nsh1106_cmd(0x00 + (col & 0x0F));\n// high nibble\nsh1106_cmd(0x10 + ((col >> 4) & 0x0F));\n```\n\nAfter that it was a regular I2C transmission, setting the address, marking the command with `0x40` and actually sending the data to update pixels on the screen.\n\nAs an aside, I got a bit confused at this point due to the way data was prepared. The width, for example, was computed first as `64` and then shifted over by 3 bits, which leaves the upper 5 bits and a value of `8`. But this was still referred to as `width` in code. There were a couple oddities like this.\n\nAfter looking through everything, I got what I figured was a general sense for how the send logic works.\n\nTo set a pixel, you would have to:\n- start at the correct page address out of the 8 available pages\n- set the correct column address\n- transmit all pixels to modify as bytes until the end of the page (64 bytes)\n\nThis was my _very rough_ understanding at this point, so I wanted to practically apply it and see what kind of results I could get to test that understanding.\n\n### Giving it a spin\n\nAfter a lot of fiddling around, I had some code that should have done _something_, but I wasn't getting any response from the OLED. Just to be sure I was communicating I checked SDC & SCL with an oscilloscope (the purpose of the probes from the picture above). Curiously enough, I was reading 3v & nearly _2v_ on the SDA & SCL pins (I didn't compare which voltage was on which pin at this time, but already this was a problem).\n\nThe first thought that crossed my mind was that I was drawing a bit too much power through the GPIOs on the ATTiny13, especially since I had the pins wired up directly to the OLED while simultaneously driving some LEDs.\n\nJust to do a little verification, I went back to the Arduino Uno that I tested with to measure the pin voltages there. I wasn't really expecting anything other than 5v across the board, but I wanted to be sure I wasn't missing something. Of course everything measured 5v and worked as it had before.\n\nBack over at the ATTiny13 I was able to confirm that I was reading specifically 2v on SCL & ~3v on SDA. As a quick test, I swapped my power & ground to a different rail, disconnected all the LEDs I had hooked up, and before I removed the associated LED driver code I checked the voltage levels again. Spot on this time around 5v. I was just pulling too much power from the GPIOs directly. Going forward I noted that I would need some transistors to help do the work for drivings LEDs and other peripherals.\n\n> [!NOTE]\n> Not having a formal background in embedded systems design & development gets me like this all the time (regarding the overdraw on GPIO). Still I find it quite enjoyable to really drill down, figure out what's going on at the wire level, and learn something new in turn for next time!\n\n### Getting Pixels on the Screen\n\nComing back after about a few weeks, I picked up where I left off and tried to get some actual pixels on the screen; now that I had the right voltages. Maybe I was writing into a strange corner or something, or maybe my I2C setup wasn't working as expected? I wasn't entirely sure yet, but I gave the existing pixel drawing code another look over. I tweaked a few things as well, but still wasn't getting anywhere.\n\nAt this point, I backed up a bit and thought about the I2C scanner that I used to confirm the device address in Arduino. It occurred to me that honestly it would be _much_ easier to try to implement the same, and use that to confirm whether my bit-banged I2C was working in the first place.\n\nI needed to check for an 'ack' (acknowledge) coming back while I polled across the possible device addresses. Effectively I just needed to write to the address with `addr << 1` to indicate we wanted to set into WRITE mode, and look for an acknowledge in return. I had to check, but I2C addresses come in the range of `0x08` to `0x77`, so I needed walk the range and see what popped up (the rest of the possible addresses appear to be reserved and were skipped for this check).\n\n### A Detour into PGM Space\n\nWell, while putting that code together, I made a couple of interesting observations. The first was with all the strings I was printing out for debugging via UART, I kept exceeding the 64 byte SRAM limit.\n\nWith a bit of investigation & research, I came across something that I really should have checked for initially, storing fixed strings in program flash & reading those instead! Turns out there's `<avr/pgmspace.h>` [^pgm] which provides functionality for working with data stored in program space for this very purpose.\n\nFor the strings that were filling up my SRAM, I was able to leverage `PSTR` combined with `pgm_read_byte`, which allows us to declare a pointer to a static string that's contained in program space & read the pointed-to data itself.\n\n[^pgm]:https://github.com/Synapseware/avr/blob/master/include/avr/pgmspace.h\n\n### Polling for I2C\n\nOnce I had resolved the SRAM issue, and I was able to leverage the extra flash space I had, I couldn't find an address for any attached I2C device. This was partly good news, since it suggested the actual SH1106 code _may_ have been okay separate from my I2C code, but it also indicated that there was something up with either my I2C implementation or the way I had wired everything together.\n\nI double checked the onboard connections between the ATTiny13 and the OLED board. And somehow, after checking, I saw that I managed to flip SDA & SCL. Honestly this happens _way too often_, even when I check for it, but I was glad that I caught it at this point. Swapping those back and rerunning the I2C address scanner gave me...`0x3C`! Fantastic! Finally I was getting a response, and the correct one as well.\n\n> [!TIP]\n> Always double check your connections! I can't count the number of times I've flipped things around, even when double & triple checking (although maybe it's just me). Often this can be as simple as correcting the misplaced connections, if you're lucky, but it can also lead to a bit of magic smoke leaving your beloved device.\n\nWith the scanner logic in place, I wanted to bring back what I was doing before with the pixel painting, and seeing whether that would work now that I had corrected my pin connections. With the original OLED driver implementation in place, I gave the code another run.\n\nAnd it finally worked!\n\n![A rather messy array of semi-organized pixels being driven by a bit-banged I2C implementation on an ATTiny13.](/assets/images/oled-on1.jpg)\n\nIt's not a particularly pretty sight, but it is functional! I messed around with the code a bit more and I was able to see that I was getting somewhat predictable results, but only for about half the screen.\n\nJust to test things out I wanted to try and clear the screen entirely, and when I ran that it was pretty clear I was really only covering half the display.\n\n![Tweaking with the code seemed to only cover about half the screen.](/assets/images/oled-on2.jpg)\n\nIt looked like I was hitting the first 64 columns, but not the rest. I just needed to correct my block logic from 0-7 to 0-15. And after that I was getting a full paint, but I wasn't able to set all the pixels yet, so there was still something else going on.\n\nI was pretty sure it was just a logic error at this point that was escaping me. So I did a quick debugging round (or two), isolated the issue, and also whipped up a nice sin-wave function to boot.\n\n![Sin-wave approximation from an ATTiny13](/assets/images/oled-wave.jpg)\n\nAnd that was it! I was pretty much done at this point, and pretty happy with the results. I still had to deal with how I wanted to display more structured visuals, glyphs & such for text, but I figured I would handle that later. The most important part was that the display was working, I had a bit-banged I2C implementation, _and_ a working but simplistic SH1106 driver.\n\nFrom here, my next steps are to add some inputs to match the outputs, and to start interacting in a more hands-on fashion with the controller.",
    "frontmatter": {
      "title": "Driving an OLED Display from an ATTiny13",
      "date": "2026-04-18",
      "tags": "[attiny, post, project, avr, oled]",
      "excerpt": "Setting up a tiny OLED driver to work within 1kb of flash & 64 bytes of SRAM on an ATTiny13",
      "draft": "false"
    }
  },
  {
    "slug": "debugging-and-monitoring-on-attiny13",
    "title": "Debugging & Monitoring on an ATTiny13",
    "date": "2026-03-07",
    "excerpt": "Getting debugging & monitoring setup with bit-banged UART on the ATTiny13",
    "content": "---\n\n_This post is a continuation from [getting started developing for the ATTiny13 & 45](/post/attiny-getting-started-and-flashing)._\n\nPreviously I was going over how we can get a simple program flashed onto an ATTiny45 (and also ended up doing this for an ATTiny 13 too), and we got an simple LED flashing. This was a great way just to verify that we could write, compile and flash the board successfully. I even played around a bit in my spare time and hooked up extra LEDs to the other IOs to create a little chain of lights, just for fun.\n\nWith the basic setup out of the way, I wanted to get a bit more information about what's going on during execution on the ATTiny itself. I already had LEDs, which could be used to broadcast a bit of information. But I also wanted to communicate data back on the wire via some clock, or even pulse back in Morse code on a single pin. Unfortunately I'm not particularly well-versed in Morse Cod, and I also preferred to have something more robust.\n\nTo that end, I opted to setup basic Serial support on the ATTiny via UART. The ATTiny doesn't have hardware support for UART, but I could implement a bit-banged version of instead. I came across this after looking around a bit for solutions, but I wasn't entirely familiar with bit-banging at first (although I'd heard about it numerous times before). Effectively, it means we can emulate the protocol in software rather than in hardware, using GPIOs in this case, to do what the hardware would be doing for us. It'll be both slower & less efficient, but for me I figured this would be sufficient to start with.\n\nWhile looking more into how I could bit-bang UART, I also came across the debugWIRE protocol. I was curious so I dug more into that. Turns out the [debugWIRE protocol](https://en.wikipedia.org/wiki/DebugWIRE) was developed by Atmel for their AVR family of microcontrollers -- it's a bit more lightweight than JTAG for the same purposes. To enable it, it does requires programming the DWEN fuse, but in doing so it also disables the RESET pin and ISP (which we're using to program the chip in the first place). Already this kind of ruled it out for the basic monitoring I wanted (combined with repeated reprogramming early on), but reading further it's clear that it provides excellent debugging support: full memory inspection, execution control via breakpoints, etc.. There is dedicated hardware that you need in order to leverage debugWIRE, but it looks like it's reasonable to build your own as well. So with all that in mind, I decided to stick with a lightweight bit-banged UART for now.\n\n### Setting up TX-only UART\n\nGiven that I've never done something like this before, and the flash constraints are pretty tight on the ATTiny boards, I wanted to start with a TX only implementation on the ATTiny13 (again with just 1KB flash). I combed the web a bit to find existing implementations, and I also used Claude Code to help generate a few small proposals to compare against. Altogether this netted the following high-level approach:\n\n- Declare the clock rate (10Mhz)\n- Declare what we want as our desired baud rate (something slow, like 9600 baud)\n- Declare our TX pin (pretty much the only pin we'll need for a TX-only implementation)\n- Sketch out a software implementation of the protocol that we can repeat for each byte\n\t- Pull the TX line low for our starting sequence\n\t- Shift out all 8 bits\n\t- Pull the line high again for our stop sequence\n\nThat's pretty much it! Not too bad when you abstract the implementation details away.\n\nWriting up a candidate implementation that followed that plan, I got something like this:\n\n```c\n#define TX_PIN PB0\n#define BAUD_RATE 9600\n\n// try to setup a timing delay factoring in the ATTiny13 clock\n// 10Mhz in our case\n#define BIT_DELAY_US (10000000 / BAUD_RATE)\n\n/**\n * Initialize serial pins\n */\nvoid serial_init(void) {\n  // TX output\n  DDRB |= (1 << TX_PIN);\n  // pull TX high (idle)\n  PORTB |= (1 << TX_PIN);\n}\n\n/**\n * Write a single byte via bit-banged serial\n */\nvoid serial_write_byte(uint8_t data) {\n  // send out the starting bit\n  PORTB &= ~(1 << TX_PIN);\n  // wait\n  _delay_us(BIT_DELAY_US);\n\n  // shift out our bits\n  // least-significant bit first\n  for (uint8_t i = 0; i < 8; i++) {\n\t  if (data & 0x01) {\n\t\t  PORTB |= (1 << TX_PIN);\n\t  } else {\n\t\t  PORTB &= ~(1 << TX_PIN);\n\t  }\n\t  _delay_us(BIT_DELAY_US);\n\t  data >>= 1;\n  }\n\n  // send the stop bit\n  PORTB |= (1 << TX_PIN);\n  // closing wait\n  _delay_us(BIT_DELAY_US);\n}\n\n/**\n * Helper to write out a whole string of bytes via our serial implementation\n */\nvoid serial_write_string(const char* str) {\n  while (*str) {\n\t  serial_write_byte(*str++);\n  }\n}\n\n```\n\nTo quickly summarize what I'm doing in the code above, this is a _TX-only_ implementation that pulses about 9600 times per second (9600 baud). We can achieve this by dividing up the number of clock cycles we have per second (10Mhz here), to know how long we should be delaying for each pulse. The actual protocol implementation itself is pretty simple as well. You send a starting sequence (TX pulled LOW), delay, and then send one bit at a time as you shift through all 8-bits of the byte. Once done, we send a closing sequence (TX pulled HIGH), and that's it. In theory the above should work pretty well without needing to do too much else.\n\nChances are that this implementation will be a bit off in terms of timings, given it's not accounting for the extra clock cycles that are consumed by the underlying instructions themselves after compiling, but it should be small enough to be negligible. In this case the delays are about ~1041 clock cycles, so it is still possible we could drift on really long chains of outputs. But we shouldn't be entering that territory for shorter debugging messages. Still, if that comes up, it won't be entirely unexpected.\n\nWithout further ado, we're ready to test this out! As is I still was wired up to some LEDs, so there's no actual UART connection to write to yet, but I wanted to make sure the hex was going to be small enough to flash successfully.\n\n```bash\n# compile to elf\navr-gcc -mmcu=attiny13 -o main.elf main.c\n# convert to intel hex for flashing\navr-objcopy -O ihex -R .eeprom main.elf main.hex\n```\n\nFrom that, I had no issues during compilation, and the size looked okay. The one exception was from a warning that I kept seeing (from the beginning actually, I just omitted it early on).\n\n```\n# warning \"F_CPU not defined for <util/delay.h>\"\n```\n\nAnd actually, this time, I decided to follow up on that for good measure. Seems this is important to allow the build system to know how fast your chip is running (in our case 10Mhz, assuming no changes). It apparently defaults to the following, which can also be added explicitly in your source or via compiler flags to remove the warning.\n\n```c\n#define F_CPU 1000000UL\n```\n\nAnd there was another warning as well that deserved some attention:\n\n```\n# warning \"Compiler optimizations disabled; functions from <util/delay.h> won't work as designed\"\n```\n\nThis one made more sense given I had no optimizations set during compilation. I figured I would focus on optimizing for size, and once adding that in, the last warning was gone as well. To bring up what optimizations were available I checked out the help for `avr-gcc`, and tried something like this\n\n```bash\navr-gcc --help=optimize\n```\n\nThat provided a _ton_ of optimization information that I wasn't aware of the first time around, which was fantastic. Right up at the top I saw a familiar entry,`-Os`, to optimize for space. There was also `-Ofast` for speed, `-Og` for _debugging_ (interesting), and the usual `-O<number>` for tweaking how far to push optimizations. For me, `-Os` was sufficient.\n\n```bash\navr-gcc -Os -mmcu=attiny13 -o main.elf main.c\n```\n\nJust for good measure, I checked the size difference using the old program I was running (before adding in the one-way Serial logic outlined above). Before I got a binary with 294 bytes, and optimizing for space I got just _90_ bytes. This looked great at first, but upon flashing the program nothing worked correctly, specifically the LEDs turned on but never turned off. I tried out `-O1`, `-Og` and `-Ofast` just to see how those fared, and all of them produced the exact same result.\n\nThis was puzzling at first, so I went back to my implementation, and swapped out a custom `_delay_ms` function I wrote with a busy spin, back to the library defined version for AVRs. This was previously done, as a test, to try to reduce the overhead from compiling against that library. But at this point I wondered if it was better to compile it in and _then_ optimize.\n\nSure enough, then compiling with `-Os` produced the desired program functionality at just 106 bytes. But now I was curious why my 16 bytes smaller version did _not_ produce a working program under optimization. At this point, I assumed that my function implementation had been aggressively optimized out.\n\nI tweaked things a bit a first to no success, but eventually tried using `volatile` to mark the variable in my busy spin as non-optimizable to the compiler. After doing that, I got a 146 byte program (without the delay libs included) that worked as expected. So this seemed to support the rationale that the compiler was optimizing the function away. But why? Well, to elaborate on that a bit, in its current form my custom delay looked a bit like this (with volatile added to resolve the issue):\n\n```c\nvoid _delay_ms(int ms) {\n    volatile int ms2 = ms * 16;\n    while (ms2 > 0) {\n        ms2--;\n    }\n}\n```\n\nNot very pretty being a busy spin & all, but again we're just playing around with things here. Adding the volatile keyword to `ms2` helped remove the issue, and that in itself was changed from directly manipulating `ms` itself instead of using `ms2`. Exploring this a bit further I found that optimizing with `volatile int ms` in the parameter declaration also produced a working program, but at a greatly accelerated cycle speed (the LEDs were flickering on and off maybe 10-20x faster than expected). Clearly there was still more going on that I didn't understand. I didn't want to get _too_ off track so I could still have time to test out the serial logic, but I wanted to do one last look into the `_delay_ms` library implementation itself.\n\nA brief peek into **util/delay.h** showed what appear to be a couple of versions for how that function is compiled, one optimized with an internal call to `__builtin_avr_delay_cycles` and another unoptimized busy loop with a call to `_delay_loop_2`. [Looking up the first](https://gcc.gnu.org/onlinedocs/gcc-14.1.0/gcc/AVR-Built-in-Functions.html) indicates it can \"Delay execution for _ticks_ cycles\", taking that at face value without digging further seems pretty self explanatory. I found a bit more info as well under the [AVR libc user manual](https://avrdudes.github.io/avr-libc/avr-libc-user-manual/group__util__delay.html), which indicated the second approach is in-fact a fallback with a busy loop. Pretty interesting stuff!\n\nAssuming we're optimizing, we'll get the first version, and assuming that's heavily optimized then I don't really need my own implementation anyways (which probably was the right answer in the first place to begin with).\n\nWith that tangent out of the way, I came back to the TX-only feature. I compiled & flashed that to the board, while sending bytes for the sequence `hello world` (original, right?). The end result was a 226 byte hex that flashed with no issues, and as far as I could tell the TX-only logic ran as expected. Great, but it passed by so quickly there was wasn't much more than a slight delay before the still-in-place LED sequence started up. I wanted a little more visual confirmation before proceeding, and since I had a LED on PB0 (which is our TX pin), I figured I could artificially delay the normal baud rate to something ridiculously slow and watch the serial write take place.\n```c\n#define BIT_DELAY_US 1000 * 1000 \n```\n\nWith that I was able to see some activity on the LED. I had no outright reason to doubt what was setup so far, but it's always nice to get some concrete feedback _just_ to be sure. I can't count the number of times this kind of quick sanity check has helped to avoid issues later on.\n\n### Testing out one-way UART\n\nNow that we're writing data out via serial, we need something to receive the incoming data! After setting the bit delay back to what it was before, I dug out a Serial to USB converter that I had lying around. The particular one that I grabbed was converter based around an \"FTDI FT232RL\" that I got some years ago. I'd used it a handful of times for UART communication with various other devices -- you'd be surprised how many devices have UART headers with or without pins attached -- and it was my go-to this time as well. Apparently the FT232 is a pretty popular converter for USB to Serial & vice-versa, so there's no shortage of devices that incorporate it.\n\nOn macOS I didn't have any problems communicating with the FT232 driver-wise, but if you're on Windows or any other system that needs some drivers, you can pull them directly from the [FTDI chip product page](https://ftdichip.com/products/ft232rl/). You should also be able to use most other USB to Serial converters that you happen to come across, assuming the converter chips are legitimate & function as expected.\n\nWhat's cool here is that we're just checking one-way communication, so all we need to do is connect the TX pin on our ATTiny to the RX pin of our converter, and connect GND as well. The rest we can leave unconnected for now assuming the board has power. VCC might be needed if this isn't the case, but then double check that you're using the _right voltage_.\n\nAt this point I plugged the converter in, powered up the ATTiny, and used `minicom` to establish a connection to our new serial device:\n```bash\n# may need to check where your device is listed in /dev/ first\n# just checking tty devices may be enough\nminicom -b 9600 -D /dev/tty.usbserial-AQ044IRG\n```\n\nOn my end then I got a whole bunch of random symbols, which wasn't what I was expecting, but least it was a start as data was being sent over the line. I decided to slow the baud rate down to 4800, re-flash, and try again. Still the output was the same, so I dropped it down to 2400 as well, but it was still the same result. Despite this, the number of symbols that I was receiving looked roughly about right for the string I was sending, but the encoding/decoding looked off, so I started looking things up a bit.\n\n### Debugging\n\nThe first thing I came across was related to the clock speed of the device, which I was _pretty_ sure was 10Mhz, but honestly I never actually checked it to be sure. So going in that direction I went ahead and read the relevant fuses using avrdude to see what our clock was set to, being `lfuse` and `hfuse` respectively. On my end I got `0x6A` and `OxFF`.\n\nThere's [some helpful tools](https://eleccelerator.com/fusecalc/fusecalc.php?chip=attiny13a) to assist with both reading & writing fuse bits for ATTiny chips. Comparing against my own values more or less indicated I had the default fuses set, which is what I would expect given I hadn't explicitly changed any fuse values directly via `avrdude`. For the defaults, this also indicated I had an internal clock divider enabled. Taking our default 9.6Mhz (a bit off from the 10Mhz I thought we had before) and giving us about 1.2Mhz.\n\nThe question I had now was whether the internal clock was what we should be tracking with `F_CPU`, or the regular 9.6Mhz one. This brought me back to the fuses since I was pretty sure I overlooked the clock divider, and I figured I needed to turn that off to get a `9.6Mhz` clock without tweaking anything else. But before that, I did one little check with `F_CPU=1200000UL` just to see if that would work given the clock divider was still active.\n\nAnd turns out that worked as expected! Finally I was getting a nice clean bit-banged serial output, and I was pretty happy with the result.\n\nDespite this, I still wanted to remove the clock divider, and verify that I was going in the right direction. If I was right, I could get the same output with 9.6Mhz instead of 1.2Mhz, and simply adjust `F_CPU` to match once I changed the fuses.\n\n```bash\n# read lfuse just to check if it's 0x6A (w/ clock div 8 on, the default)\navrdude -p t13 -c usbtiny -U lfuse:r:-:h\n\n# if so, we can write 0x7A to disable clock div 8\navrdude -p t13 -c usbtiny -U lfuse:w:0x7A:m\n```\n\nReading the lower fuse byte one more time gave `0x7A`, just to confirm, and after adjusting `F_CPU` back to `9600000UL`, we got good reads again at the higher clock + baud. With that debugging tangent aside, I went back into looking at the UART implementation.\n\n### Looking at a full UART\n\nNow that I had the serial write, I was curious what it might take to add RX support as well. Turns out this direction is _much_ more complicated. The key detail on the RX side is that we have to be able to pick up the start sequence of an incoming byte, process each of the incoming bits, and then we have our byte. Initially this seems somewhat straightforward, until we consider what this does to the rest of our code.\n\nBeing in a single threaded environment, we're either handling TX or RX, but not both. If we try to 'listen' to an incoming RX byte, we either have to busy spin on the RX pin waiting for a change, or setup an interrupt handler. The later sounds quite good until you consider that an interrupt during a TX sequence will completely break the sequence, or if we disable interrupts during TX then we'll either miss our incoming RX byte or misinterpret a portion of it later. Either way, it's clear we're only running at half duplex (one way at a time), and not full duplex (both ways at once).\n\nWith that in mind, there was the choice of polling or using an interrupt. I opted for the interrupt given how busy spinning would absolutely ruin the ability to get anything else done, at the cost of needing to setup an ISR along with processing bytes post-receipt once the interrupt was completed. I figured that if this turned out to be _too_ complicated, I could always let it be, and simply move onto the other parts of the project I wanted to work on. Lacking the ability to send data in via RX would be a pain, but there would still be plenty of other ways to communicate with the ATTiny.\n\nI let Claude code whip up a proposal for RX handling with interrupts, and got something to test out. Initially I wanted to write this myself, but given I didn't really need this yet I was more curious _how_ a potential implementation would look; also to serve as a reference if I decided to come back to this.\n\n```c\n#include <avr/interrupt.h>\n\n#define RX_BUF_SIZE 16  // Must be power of 2\nstatic volatile uint8_t rx_buf[RX_BUF_SIZE];\nstatic volatile uint8_t rx_head = 0;\nstatic volatile uint8_t rx_tail = 0;\n\nstatic void uart_rx_init(void)\n{\n    DDRB  &= ~(1 << RX_PIN);   // Input\n    PORTB |= (1 << RX_PIN);    // Pull-up\n\n    PCMSK |= (1 << RX_PIN);    // Enable pin-change on RX pin\n    GIFR  |= (1 << PCIF);      // Clear any pending flag\n    GIMSK |= (1 << PCIE);      // Enable pin-change interrupt\n\n    sei();\n}\n\nISR(PCINT0_vect)\n{\n    // Only act on falling edge (start bit)\n    if (PINB & (1 << RX_PIN))\n        return;\n\n    // Disable pin-change interrupt during reception\n    GIMSK &= ~(1 << PCIE);\n\n    // Center on start bit\n    _delay_us(BIT_DELAY_US / 2);\n\n    // Verify still low (not a glitch)\n    if (PINB & (1 << RX_PIN)) {\n        GIMSK |= (1 << PCIE);\n        return;\n    }\n\n    // Sample 8 data bits\n    uint8_t byte = 0;\n    for (uint8_t i = 0; i < 8; i++) {\n        _delay_us(BIT_DELAY_US);\n        byte >>= 1;\n        if (PINB & (1 << RX_PIN))\n            byte |= 0x80;\n    }\n\n    // Wait through stop bit\n    _delay_us(BIT_DELAY_US);\n\n    // Store in ring buffer\n    uint8_t next_head = (rx_head + 1) & (RX_BUF_SIZE - 1);\n    if (next_head != rx_tail) {  // Drop byte if buffer full\n        rx_buf[rx_head] = byte;\n        rx_head = next_head;\n    }\n\n    // Re-enable pin-change interrupt\n    GIFR  |= (1 << PCIF);      // Clear any flags from the data bits\n    GIMSK |= (1 << PCIE);\n}\n\nstatic uint8_t uart_rx_available(void)\n{\n    return rx_head != rx_tail;\n}\n\nstatic uint8_t uart_rx_read(void)\n{\n    while (!uart_rx_available())\n        ;\n    uint8_t byte = rx_buf[rx_tail];\n    rx_tail = (rx_tail + 1) & (RX_BUF_SIZE - 1);\n    return byte;\n}\n```\n\nAnd the following in my main:\n\n```c\nuart_rx_init();\n\n// and during the main loop later\n// periodically check for bytes from the ring buffer\nif (uart_rx_available()) {\n\tuint8_t c = uart_rx_read();\n    serial_write_string(\"got a byte: \");\n    serial_write_byte(c);\n    serial_write_string(\"\\n\");\n}\n```\n\nThe buffer looked pretty tight, but I still wanted to give this a shot and at least see whether it could. Flashing the program showed I was using 512 bytes (up from 260 with just the TX code). So already that nearly doubled my program size just to get RX logic via interrupts.\n\nAlmost *immediately* I realized I didn't wire anything up to allow this to work. So I plugged in PB1 to the RX pin on my serial converter, and then went back to minicom to check the output. I was surprised to not only not be able to send anything, but also now I wasn't getting any output either. My first guess was an extra `DDRB` line that I had forgotten to remove.\n```c\nDDRB |= (1 << PB1);\n```\n\nThis was immediately after the RX pin setup, which was flipping the RX pin back to an input erroneously. I corrected that plus made sure to skip PB1 when setting LEDs I had setup.\n\nStill no good, but I was pretty sure the main loop wasn't blocked by either the available check or the RX read, given the LED sequence I had programmed before was still running. So I disabled the RX setup entirely to see if that was related somehow.\n\nStill nothing, so I disabled the RX checking entirely, leaving the original code more or less intact.\n\nAnd still I got nothing. So I backed it up all the way to commenting out all of the new RX code entirely, just to be sure I hadn't messed something else up.\n\nAh! At this point not only was I getting serial output again, but the byte count dropped back to around 260 for the flashed program. Curious, I wanted to just let the interrupts be included and to watch what might happen. I would assume the library would be totally optimized out if everything was working as expected, and it was upon testing. Moving onto the next bit I wanted to bring back to the static vars, as I was wondering if they had disabled some compiler optimization that was causing issues then.\n\nNow I was getting somewhere, the program size increased from 262 to 278 (+16 bytes) _and_ it was no longer working correctly for serial output. At this point I was pretty confident that the usage of volatile on a globally scoped variable was messing with the compiler. Slowly I started to pare things back to just a single entry, `rx_head`, no issues.\n\nI did the same with `rx_tail` as well, also no issues. And then I re-added the little ring buffer `rx_buf`. Still no issues.\n\nOkay, so at this point I was really starting to wonder _what_ exactly was going on. My next thought was that maybe I was exceeding what we could reasonably store in program. A quick look back at the data sheet reminded me I only had 64 bytes of SRAM to work with. With our ring buffer byte array, we had an extra 16 bytes being put aside, not including the space for the `rx_buffer` pointer itself. I figured that with all of those variables together we were just over the edge of our SRAM limit.\n\nWhat was really interesting, and something I hadn't thought of before, was that I never got _any errors_ about this upfront. For example, when compiling a basic Arduino sketch, flash & RAM consumption from globals are reported. This is generated via static analysis, and ideally we should be able to get the same from avr-gcc too via a flag, or at least that's what I thought.\n\nWell, it turns out there's another utility called `avr-size` that does exactly this! I ran it through on my main.elf like so:\n\n```bash\navr-size --mcu=attiny13 -C -x main.elf\n```\n\nand with just the two global volatiles added I got the following:\n\n```\nAVR Memory Usage\n----------------\nDevice: attiny13\n\nProgram:     278 bytes (27.1% Full)\n(.text + .data + .bootloader)\n\nData:         50 bytes (78.1% Full)\n(.data + .bss + .noinit)\n```\n\nFantastic! This was exactly what I was missing, and armed with this new information I was able to see that with the ring buffer I was topping out at 66 bytes for SRAM (103.1% full), definitely not viable. Just to further validate this was the case I trimmed down the buffer by 2 to fit into 64 bytes. That re-flashed with no problems and I was getting the correct serial output again.\n\n![Minicom output showing the string \"Hello World\" being received from the UART](/assets/images/comms-hw.png)\n\n![Similar output with some different character ranges for testing.](/assets/images/comms-testing123.png)\n\nAfter re-enabling the RX logic and slimming down that longer test string I was writing out, I had a program which weighed in around 472 bytes flash and 38 bytes data. Not too bad, plus the receive logic was working finally too for just a single byte at a time. Not the most efficient, but that's still a functional half duplex UART along with some other logic for around 472 bytes.\n\n![Minicom output showing that writing along with reading for a single byte, as a PoC.](/assets/images/comms-read-write-2.png)\n\nThat'll cap this one off, as it was getting quite long. But I already I was having quite a lot of fun working through this & seeing what I can get setup on this little microcontroller. I think I'll probably conditionally compile out the RX handling going forward, unless I really need it, especially since I'm just getting 1 byte at a time, and the program space could definitely be used for something else.",
    "frontmatter": {
      "title": "Debugging & Monitoring on an ATTiny13",
      "date": "2026-03-07",
      "tags": "[attiny, post, project, avr, debugging]",
      "excerpt": "Getting debugging & monitoring setup with bit-banged UART on the ATTiny13",
      "draft": "false"
    }
  },
  {
    "slug": "attiny-getting-started-and-flashing",
    "title": "Getting Started Developing for the ATTiny13 & 45",
    "date": "2026-02-14",
    "excerpt": "Getting started with writing & flashing to an ATTiny 13 & 45 microcontroller.",
    "content": "In this post I'll go over my personal experience getting setup developing & flashing the ATTiny13 and 45 microcontrollers.\nThis is mostly a self-documented process for my own purposes, but I've decided to share it for others to follow along as well.\nI'll be writing this up as I work through things so I can capture as much of the original process as I can.\nPersonally I have an _excessive_ amount of these kinds of notes & dev logs locally, but they're in absolutely no shape to be shared.\nThis is a shame really, so I figured I would take the extra effort to just write my notes in a form that _is_ suitable for sharing in the first place.\nBeyond that I have further goals here, but for this post we're going to start small with getting setup to development.\nI just want to get to a point where I can reliably program the microcontroller, so let's start with that.\n\nFor some background as to why I'm doing this, I've been pretty interested in building up dioramas over the past few years, with a bit of increasing complexity on each following year. Most of it is a creative outlet, an enjoyable form of self-expression, but another part is also technical. In particular, I'm quite interested in experimenting with little technical touches: lights, servos, buttons, and connectivity (where it makes sense) to bring a diorama into a slightly more interactive space. There do already exist solutions for this exact kind of thing, but the last point is that I'm using this as a personal learning experience. By starting with a couple standalone microcontroller chips  (ATTiny13 & 45 for now), I can experience the joy & excitement of watching the whole system come together piece by piece.\n\nThere will _definitely_ be mistakes during this process, and that's both intended and embraced. I'm very much looking forward to seeing where the project takes me, what I learn along the way, and the experience as a whole. The end goal is ultimately more practical in nature, I would love to have a custom-built prototyping board that I can utilize in other projects down the road, but it's very much about the journey at this stage (that'll come later on).\n\nAnyways, for starters we're going to get a simple ATTiny45V-10PU (in an 8-pin DIP package) and an ATTiny13, both setup with the all too classic _hello world_ example for microcontrollers. That is, a flashing LED. This will also save me the effort of figuring out later on whether I zapped the chip by accident while handling it, as I may or not have been using proper ESD protection at the time. Fingers crossed in that regard.\n\nI'm not particularly fixed to using just this microcontroller (I have several more ATTiny variants, plus one or two from other brands), but it's one of the smaller simpler ones I have around. It also happened to be at the top of my parts box, and I don't have any pressing need (yet) to start comparing the differences between boards. Quite frankly I don't even _know_ exactly what I'll be exactly needing, as I only have a rough idea of where this project will go. Starting with something concrete gives me a spot to work from, and to later compare practical experience with what I could be getting from available alternatives.\n\nSo, I needed to familiarize myself with the board, sure I bought it and already knew what I was picking up _generally_ speaking, but I hadn't yet dove into it in detail. I don't intend to go too far in this first step, but I do want to:\n- program the ATTiny\n- toggle a LED on some IO pin for verification\n- _maybe_ read out on Serial back once we're done\n\nAgain the flashing LED is very much a \"hello world\" example, but also a simple way to ensure that at least the chip is good to go, and isn't toast. It also gives me a way to verify that I've got a working setup to program AVR microcontrollers. So let's get into that part.\n\nBeyond knowing what kind of chip you're building for, you'll also need the AVR toolkit -- specifically `avrdude` and `avr-gcc`. You can flash these chips using an Arduino as an ISP (assuming it's got an AVR chip in it), or also with a regular AVR programmer. For this case I'll just be working with a dedicated AVR programmer, assuming that works out as expected.\n\nI _have_ thought about exploring programming these boards in [Zig](https://ziglang.org/) rather than C, as I've been picking that up in my spare time. I've always had a soft spot for C (and a bit of C++, but less so), but I don't miss the footguns. I was more interested in the direction of Rust, which is an _excellent_ language in terms of safety (especially with the borrow checker), but I'm finding I naturally gravitate towards a lower level of abstraction. In that sense, the way Zig exposes lower level constructs -- such as making safety checks a present but optional aspect of the language design -- fits what I'm looking for. This isn't to suggest that it's inherently better or worse, it's just a subjective & stylistic preference on my end.\n\nBack onto the topic of `avrdude` & `avr-gcc`. A couple of years ago I was working with ATTiny13 and an ATTiny16 on a separate project. I figured I toasted the '13 sadly, but I was able to get the '16 working as a near drop-in replacement. During that process I took quite a few notes about how to get thing setup, wired & programmed.\n\n_Just an aside_, much later in this process I went back to that very board with '13 on it, and pulled it off with a hot air gun just to check it _one more time_. And it actually still worked! Either I completely overlooked something before, or crosswired something on the board it was attached to, but either way it was an extra chip on hand that I didn't expect to have.\n\n### First Program\n\nSo, for starters, we're going to need to lookup how to get started with setting up a simple test program for the ATTiny. Thankfully, there's an abundance of tutorials on how to get setup with writing, building & flashing for AVR microcontrollers. For our case here we just want a simple program to start with\n\n```c\n#include <avr/io.h>\n#include <util/delay.h>\n\nint main()\n{\n\t// set pin PB0 on ATTiny45 as output\n    DDRB |= (1 << PB0);\n\n    while (1)\n    {\n\t    // toggle PB0 every 1 sec\n\t    PORTB ^= 0x1 << PB0;\n        _delay_ms(1000);\n    }\n    return 0;\n}\n```\n\nThis will just allow us to toggle a pin high/low every second. That's more than enough for us to wire up a LED for testing, or even just to measure the output with a multimeter or oscilloscope.\n\n#### An aside on GPIOs\n\nAlso, regarding GPIOs, we only have _6_ on both the ATTiny13 & 45. Specifically, these are PB0 - PB5. The other 2 pins are GND & VCC, however many of these GPIO pins have alternative functions that I'll want to leverage later, such as I2C and SPI for communicating with other peripherals. With that being said, I planned to start with PB0.\n\nIf we look at a quick diagram, we can also see where our pin is in relation to the board layout. For that we can grab the data sheet for the AVR ATTiny45V-10PU, the ATTiny13, or any other particular variant. A quick search online will turn up numerous sources to get the right datasheet.\n\nFrom the datasheet we can sketch the pinout here:\n\n```\nATTiny45V-10PU (DIP)\n\nPB5/Reset -|1 • 8|- VCC\nPB3       -|2   7|- PB2\nPB4       -|3   6|- PB1\nGND       -|4   5|- PB0\n```\n\nWe'll want to keep that reset pin in mind. It's technically a 'weak' GPIO (not entirely sure what that means, but it's stated in the datasheet & I'm assuming we just can't drive much with it), but we'll want to retain a way to reset the board. That need may change, but for now that brings us to _5_ GPIOs we can work with.\n\nIf you're curious, you can also peek at the rest of the labels assigned to these pins, which I'll unroll here.\n```\nATTiny45V-10PU (DIP)\n\n1) PB5 (PCINT5 / RESET / ADC0/ dW)\n2) PB3 (PCINT3 / XTAL1 / CLK1 / OC1B / ADC3)\n3) PB4 (PCINT4 / XTAL2 / CLKO / OC1B / ADC2)\n4) GND\n5) PB0 (MOSI / DI / SDA / AIN0 / OC0A / OC1A / AREF/ PCINT0)\n6) PB1 (MISO / DO / AIN1 / OC0B / OC1A / PCINT1)\n7) PB2 (SCK / USCK / SCL / ADC1 / T0 / INT0 / PCINT2)\n8) VCC\n```\n\nAgain if this is for the ATiny13 or another ATTiny 8-pin variant, the pinout is likely to be pretty similar.\n\n### avr-gcc\n\nUsing the program we wrote up above, we'll can compile that using `avr-gcc` (again, assuming you're installed the related AVR tools). If you haven't already, and you're on mac, you use homebrew with [homebrew-avr](https://github.com/osx-cross/homebrew-avr). If you're on Ubuntu or another Debian-like system, you can use:\n```bash\n# get avr-gcc compiler setup\nsudo apt-get install gcc build-essential\nsudo apt-get install gcc-avr binutils-avr avr-libc gdb-avr\n\n# get avrdude for flashing later\nsudo apt-get install libusb-dev\nsudo apt-get install avrdude\n```\n\nFor Windows it looks like you can pull the compiler from [microchip's website](https://www.microchip.com/en-us/tools-resources/develop/microchip-studio/gcc-compilers) directly, and a pre-built release can be retrieved for [avrdude](https://github.com/avrdudes/avrdude).\n\nAlright, assuming we have the compiler in place, we can compile it like so:\n```bash\n# assuming your program is written in main.c\navr-gcc main.c\n```\n\nFor me, this immediately failed, but I got a useful error:\n> main.c:7:5: error: 'DDRB' undeclared (first use in this function)\n\nRight, so it looks like we're missing definitions, which we should probably double check. What we're looking for is probably related to the fact we're not targeting the right board, and therefore getting its associated definitions.\n\nAfter poking around a bit I recalled needing the `-mmcu=attiny13` flag before to specify the targeted microcontroller variant, so trying that with `-mmcu=attiny45` (or `-mmcu=attiny13`) as a guess gives us a good build result.\n```bash\navr-gcc -mmcu=attiny45 main.c\n```\n\nThere may be a better option from the available values for the MCU, but for now this should suffice (we can always go back & check later). I do have a couple warnings still, but I'll come back to them later.\n\nAt this point we should have an `a.out` present as our compiled artifact. If so, we can proceed to getting ready to flash this thing.\n\n### avrdude\n\nFor starters, we're going to leverage `avrdude` here, specifically something akin to:\n```\navrdude -p ? -c ? -P ?`\n```\nIt's full of question marks, because, well, I'm genuinely _not_ certain what we need to pass yet, so let's fill in the gaps.\n\nThe `-p` flag is used to specify the part number, which we need to lookup. We can do this quickly via `avrdude -p ?` to list all available parts (literal `?` here, not a placeholder). Note that on my setup working in zsh, I had to escape the `?` to avoid it being literally interpreted, just a small thing to keep in mind.\n\nGrepping through with `attiny45` gave the following line:\n```\nt45        = ATtiny45, ATtiny45V (SPM, ISP, HVSP, debugWIRE)\n```\nWhich looks spot on! So `t45` is our part.\n\nThe `-c` flag tells us which programmer we intend to use. Much like the `-p` flag, we can run `avrdude -c ?` to list all available programmers.\n\nAnd honestly, at first glance I had _no idea_ which programmer to select. I vaguely remember having the same problem some years ago, so I flipped back through some of my old notes, and read that I was using `ft232r`, which corresponds to this entry:\n```\nft232r = FT232R based generic programmer (TPI, ISP)\n```\n\nThis turned up a huge blank in my mind at first, so I had to look it up. Turns out this was just a regular USB to Serial converter, with that particular chip being at the heart of it, which matches up.\n\nI also had a couple other AVR-based programmers that I wanted to try out this time around too. Specifically one unmarked programmer with an `ATMega8A AU 1801` and another marked as an `AVR Pocket Programmer` by Sparkfun with an `ATTiny2313-20SU` at the heart of it. The later was given to me by a friend from a while ago, but I never got to put it to use.\n\nSo looking it up a bit, I [found a product page](https://www.sparkfun.com/pocket-avr-programmer.html), and an [accompanying github project as well](https://github.com/sparkfun/Pocket_AVR_Programmer?tab=readme-ov-file). Through that, I found that `-c usbtiny` is the matching programmer field (makes sense given the ATTiny at the core). I also noted that it's got a nifty target board power feature. This lets the programmer also power the board with up to 500mA, meaning we don't have to power the board separately in addition to the programmer being hooked up. So at this point I decided to just stick with the `AVR Pocket Programmer`.\n\nThat left just the last flag `-P` for the port. Granted we don't quite have that yet until we plug in the device, so doing that and looking through `/dev` yielded....\n\nNothing, at least not at first. But before looking that up I wanted to double check that what we had so far was correct:\n```bash\navrdude -p t45 -c usbtiny\n```\n\nRunning that gave me the following:\n```\nError: cannot find USBtiny device (0x1781/0xc9f)\nError: unable to open port usb for programmer usbtiny\n```\n\nWhich looks pretty good for now, given we're not specifying the port yet.\n\nI also noted a _ton_ of warnings about an inability to locate expected data, ex. `Error: unable to locate ATxmega16A4U's data in multi-memory address space`. Looking it up suggested some issues with the installed AVR board package, and also with the configuration file itself. On my system, I was able to find several config files in various locations, and also I was able to feed them in via `-C other.conf`. This yielded fewer but more problematic errors, but at least seemed to suggest that perhaps the default configuration was the issue.\n\nMy installation was via homebrew, and looking that up didn't get me anywhere too quickly. Not wanting to get bogged down, I noted it here as I wanted to get along to the actual flashing.\n\nLooking at the programmer, the need thing we need to do is connect the dots. I wasn't terribly familiar with the pinout orientation, but it's laid out like so on the programmer I was using.\n```\nMISO |• •| VCC\nSCK  |• •| MOSI\nRST  |• •| GND\n```\n\nThe first step was verifying the orientation of VCC and GND with a multimeter, and once I had that the rest of the pins fell into place pretty nicely. It always helps to double check (or even triple check) your pins, as I often and _frequently_ end up with an off-by-one error with my wiring.\n\nAssuming that's all wired up and your programmer of choice is plugged in, you can hook everything up. One particular note about my programmer was about the board power capability. I wanted this off at first (although I soon realized a mistake with that), but later on it could be particularly problematic given it's fixed at 5v power only. If there was anything wired up to VCC expecting 3.3v, that could cause some problems. I had a few logic level converters lying around, but for now I just keep this potential issue in mind, as it wasn't a factor yet.\n\nSo, plugging in the programmer, I finally checked out the list under `/dev`, but upon further inspection realized that's not really needed, as `avrdude` _should_ pick up the attached programmer automatically (although later I did add `-P usb`).\n\nIn my case I did get a different response with the programmer connected to my computer, but it was an error:\n```\navrdude -p t45 -c usbtiny\n...\n\nError: initialization failed  (rc = -1)\n - double check the connections and try again\n - use -B to set lower the bit clock frequency, e.g. -B 125kHz\n - use -F to override this check\n```\n\nI ran again with a suggested change to the clock frequency, but got the same error. So I changed up how I connected the programmer to my computer, as it was going through a bit of overhead via a USB hub & extra cabling. Running directly attached I still got the same error, which appeared to rule the hub out.\n\nI dove back into the docs, and tried to figure out the issue from there (while not ruling out the old programmer quite yet, as it had been sitting in a box for quite some years). I wasn't able to get too far, but I _was_ expecting to at leas see a device entry pop up in `/dev` but never got that. This was rather suspicious, although it did seem that despite this `avrdude` was picking up that there was _something_ connected. I assumed I must have missed a detail there or just didn't understand what was going on yet.\n\nTo avoid spending too much time on the one programmer, I decided to try out the other programmers I had, without connecting to the ATTiny45, just to see if there was a difference when flashing. The other ATMega based programmer I had seemed to have a similar issue, but the FT232R based programmer _did_ register as a new entry in `/dev`, which matched up with what I was expecting. That gave me some sense that I could probably use that programmer to proceed, but now I was curious why the others weren't working as expected, so I dug further into that. Later on I would figure out that the FT232R programmer was definitely _not_ the right choice.\n\nBased on what I came across, not seeing a USBTiny device in `/dev` was actually expected, but I didn't understand why at first. Apparently this is due to not populating a new device entry. I'm not familiar enough to do more than speculate, but it appears to be related to how it communicates via `libusb`, but I could be wrong here. Establishing this at least alleviated my concern that the problem was from the missing device, and helped me to get a sense of why `avrdude` was indicating a device was present even when I couldn't confirm it myself. Still, none of this got to me flashing the microcontroller successfully.\n\nAt this point I got super stuck, and it was late, so I read up on things & tinkered for a bit before wrapping up for the day.\n\n<!-- - FT232R is not the right choice (that's UART, not SPI, which is COPI/CIPO)\n- Arduino nano as programmer seems viable as well... -->\n\n<!-- So after much digging & such, I decided to shift to using an Arduino as ISP, with a pair of Nanos I had lying around. One was viable, but the other was locked up due to something I had loaded on it years ago. I figured a good way to test ISP capability was to try and reset that other board (if it could be done). So I wired them up and gave it a shot after loading the first board with the ArduinoISP sketch.\n\nHowever, after much testing, I was still nowhere closer to a solution. So I took a break and stepped away for a day or so. -->\n\n### Coming back to it\n\nThe following day I read up quite a bit more about programming ATTiny's in general, and especially about some of the 'gotchas' that tend to come up. For the most part, it seems like it shouldn't have been a driver issue (but that can be investigated with another machine), and I didn't believe I messed up the pin connections (hopefully). I did have a hunch that my wires (via dupont connectors) maybe weren't as good as thought they were. That, or the microcontroller itself was possibly damaged.\n\nI was able to test with another microcontroller to see if that was a factor, and I got a similar response at first, but later made a bit of progress:\n```\nAVR device initialized and ready to accept instructions\nDevice signature = 00 00 00 (retrying)\nDevice signature = 00 00 00 (retrying)\nDevice signature = 00 00 00\nError: invalid device signature\nError: expected signature for ATtiny45 is 1E 92 06\n  - double check connections and try again, or use -F to carry on regardless\n```\n\nThis was better than before at least, but still not a success (not yet).\n\nWell after rewiring back to the AVR Pocket Programmer a second time around, I was able to get the device to connect correctly, and program with a flashing light! Finally.\n\nAfter double checking a few things, I was able to isolate it down to the power feature being set to 'off'. I was under the impression that, since I was reading voltage around ~4.7 volts on VCC, it was sufficient to allow programming to happen. _But_, it seems that although I was reading this voltage via a multimeter, I wasn't actually sending sufficient power to flash the chip. Honestly, I'm not sure yet, but I'm keeping it as an open point to investigate in more detail later on. For now having the chip flashed was a good enough spot to stop on.\n\nJust for good measure I ran the same flash sequence on another ATTiny45 I had lying around, and it was a success as well. 2 for 2!\n\nI had also tried out an Arduino Nano as an ISP programmer while I was debugging things. I suspect if I switched back to the Arduino Nano as an ISP I would be able to get a similar result as well, assuming that the chip was powered up during the flashing process like before.\n\nSo with all that we should have a simple program flashed & running on the ATTiny, but not much more than that. However with this we can focus on actually writing up & testing programs, with the next step being to have some monitoring to diagnose problems as they arise.",
    "frontmatter": {
      "title": "Getting Started Developing for the ATTiny13 & 45",
      "date": "2026-02-14",
      "tags": "[attiny, post, project]",
      "excerpt": "Getting started with writing & flashing to an ATTiny 13 & 45 microcontroller.",
      "draft": "false"
    }
  }
]

export function getPost(slug) {
  return posts.find(post => post.slug === slug)
}

export function getAllPosts() {
  return posts
}
